# Drishtiai

🦯 **DRISHTI AI** — Real-time browser-based AI assistant for visually impaired individuals. No app. No hardware. Just a browser tab and independence.

## Repo status (runnable setup)

This repository now includes:
- FastAPI backend WebSocket endpoint: `backend/routers/scene.py` (`/ws/scene`)
- Backend entrypoint: `backend/main.py`
- Backend deps: `backend/requirements.txt`
- Frontend component: `frontend/src/components/Camera.tsx`

### Run backend

See `backend/README.md`.

### Run frontend

See `frontend/README.md`.
