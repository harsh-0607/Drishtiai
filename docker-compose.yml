services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      MONGODB_URI: mongodb://mongodb:27017
      MONGODB_DB: drishti
      REDIS_URL: redis://redis:6379/0
      FRONTEND_ORIGINS: http://localhost:5173
    ports:
      - "8000:8000"
    depends_on:
      - mongodb
      - redis

  frontend:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    ports:
      - "5173:5173"
    environment:
      # Vite can read this if you later switch to explicit backend URL usage
      VITE_BACKEND_URL: http://localhost:8000
    depends_on:
      - backend

  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  mongo_data:
